							TERMWORK 3
CREATE TABLE FLIGHTS(
	FNO INTEGER,
	FROMPLACE VARCHAR(30),
	TOPLACE VARCHAR(30),
	DISTANCE INTEGER,
	DEPARTS DATE,
	ARRIVES DATE,
	PRICE INTEGER
);

INSERT INTO FLIGHTS VALUES(1 , 'BELGAUM' , 'BANGALORE' , 1000 , '2022-09-23' , '2022-09-24' , 21000 );


CREATE TABLE AIRCRAFT
(
	AID INT,
	ANAME VARCHAR(20),
	CRUISING_RANGE INTEGER,
	PRIMARY KEY(AID)
);

INSERT INTO AIRCRAFT VALUES(600 , 'F1JET' , 1000);


CREATE TABLE EMPLOYEES
(
	EID INTEGER,
	ENAME VARCHAR(20),
	SALARY INTEGER,
	PRIMARY KEY(EID)
);

INSERT INTO EMPLOYEES VALUES(1000 , 'PRITHAM' , 35000);

CREATE TABLE CERTIFIED 
(
	EID INTEGER,
	AID INTEGER,
	PRIMARY KEY(EID , AID),
	FOREIGN KEY(AID) REFERENCES AIRCRAFT(AID),
	FOREIGN KEY(EID) REFERENCES EMPLOYEES(EID)
);

INSERT INTO CERTIFIED VALUES(1000 , 601);

























																	TERMWORK 4


CREATE TABLE SALESMAN
(
	SALESMAN_ID INTEGER,
	NAME VARCHAR(20),
	CITY VARCHAR(20),
	COMMISSON INTEGER,
	PRIMARY KEY(SALESMAN_ID)
);

INSERT INTO SALESMAN VALUES(1000 , 'PRITHAM' , 'BANGALORE' , 2000);
 INSERT  INTO SALESMAN VALUES(1001 , 'AMIT' , 'MUMBAI' , 2500);
INSERT  INTO SALESMAN VALUES(1002 , 'SUHAS' , 'JABALPUR' , 4500);


CREATE TABLE CUSTOMER
(
	CUSTOMER_ID INTEGER,
	CUST_NAME VARCHAR(20),
	CITY VARCHAR(20),
	GRADE INTEGER,
	SALESMAN_ID INTEGER,
	PRIMARY KEY(CUSTOMER_ID),
	FOREIGN KEY(SALESMAN_ID) REFERENCES SALESMAN(SALESMAN_ID) ON DELETE CASCADE
);
INSERT INTO CUSTOMER VALUES(1 , 'ROHAN' , 'BANGALORE' , 9 , 1000);
INSERT INTO CUSTOMER VALUES(2 , 'PRIYA' , 'BANGALORE' , 5 , 1001);
 INSERT INTO CUSTOMER VALUES(3 , 'PRAVEEN' , 'MUMBAI' , 8 , 1000);

CREATE TABLE ORDERS
(
	ORDER_ID INTEGER,
	PURCHASE_AMT INTEGER,
	ORD_DATE DATE,
	CUSTOMER_ID INTEGER,
	SALESMAN_ID INTEGER,
	PRIMARY KEY(ORDER_ID),
	FOREIGN KEY(SALESMAN_ID) REFERENCES SALESMAN(SALESMAN_ID) ON DELETE CASCADE,
	FOREIGN KEY(CUSTOMER_ID) REFERENCES CUSTOMER(CUSTOMER_ID) ON DELETE CASCADE
);

INSERT INTO ORDERS VALUES(2000 , 200000 , '2002-09-23' , 1 , 1000);
INSERT INTO ORDERS VALUES(2001 , 300000 , '2002-09-24' , 2 , 1000);
INSERT INTO ORDERS VALUES(2003 , 45000 , '2002-09-25' , 3 , 1001);


QUERY

2-------------->
SELECT S.SALESMAN_ID , S.NAME FROM SALESMAN S , CUSTOMER C
    -> WHERE S.SALESMAN_ID = C.SALESMAN_ID
    -> GROUP BY S.SALESMAN_ID
    -> HAVING COUNT(*)>=2;

3---------->
mysql> SELECT C.CUST_NAME AS CNAME  , S.NAME AS SNAME , PURCHASE_AMT AS AMT
    -> FROM SALESMAN S, CUSTOMER C , ORDERS O
    -> WHERE S.SALESMAN_ID = O.SALESMAN_ID AND O.CUSTOMER_ID = C.CUSTOMER_ID AND PURCHASE_AMT > 4000;



4---------->
DELETE FROM SALESMAN WHERE SALESMAN_ID = 100;




























		
																TERMWORK 2

CREATE TABLE TEAM
(
	TID INT,
	TNAME VARCHAR(20),
	CITY VARCHAR(20),
	COACH VARCHAR(20),
	CPID INT,
	FROM_DATE DATE,
	PRIMARY KEY(TID)
);


CREATE TABLE PLAYER
(
	PID INT,
	PNAME VARCHAR(20),
	POSITION VARCHAR(20),
	SKILL_LEVEL INT,
	TID INT,
	PRIMARY KEY(PID),
	FOREIGN KEY(TID) REFERENCES TEAM(TID)
);


ALTER TABLE TEAM 
ADD CONSTRAINT FK1 FOREIGN KEY(CPID) REFERENCES PLAYER(PID);


CREATE TABLE INJ_RECORDS
(
	PID INT,
	DESCRIPTION VARCHAR(20),
	IDATE DATE,
	SEV_LEVEL VARCHAR(20),
	PRIMARY KEY(PID , DESCRIPTION),
	FOREIGN KEY(PID) REFERENCES PLAYER(PID)
);


CREATE TABLE GAME(
	HOST_TEAM INT,
	GUEST_TEAM INT,
	SCORE INT,
	GDATE DATE,
	PRIMARY  KEY(HOST_TEAM , GUEST_TEAM),
	FOREIGN KEY(HOST_TEAM) REFERENCES TEAM(TID),
	FOREIGN KEY(GUEST_TEAM) REFERENCES TEAM(TID)
);






























TERMWORK 2




CREATE TABLE FACULTY
(
	FID INT PRIMARY KEY,
	NAME VARCHAR(30),
	SALARY INT,
	DESGINATION VARCHAR(30),
	DNO INT
);


CREATE TABLE DEPARTMENT 
(
	DNO INT PRIMARY KEY,
	DNAME VARCHAR(30),
	DLOC VARCHAR(30),
	HOD_FID INT
);

ALTER TABLE FACULTY
ADD CONSTRAINT FK1 FOREIGN KEY(DNO) REFERENCES DEPARTMENT(DNO);




ALTER TABLE DEPARTMENT
ADD CONSTRAINT FK68 FOREIGN KEY(HOD_FID) REFERENCES FACULTY(FID);


CREATE TABLE STUDENT
(
	USN VARCHAR(30) PRIMARY KEY,
	SNAME VARCHAR(30),
	PHNO VARCHAR(30),
	CITY VARCHAR(30),
	DNO INT
);


CREATE TABLE SUBJECT(
	SUBCODE VARCHAR(30) PRIMARY KEY,
	SNAME VARCHAR(30),
	TYPE VARCHAR(30),	
	CREDIT INT
);

CREATE TABLE TEACHES
(
	FID INT,
	SUBCODE VARCHAR(30),
	PRIMARY KEY(FID , SUBCODE),
	FOREIGN KEY(FID) REFERENCES FACULTY(FID),
	FOREIGN KEY(SUBCODE) REFERENCES SUBJECT(SUBCODE)
);


CREATE TABLE TEST
(
	TEST_NO INT PRIMARY KEY,
	TDATE DATE
);




CREATE TABLE SCORE
(
	USN VARCHAR(30),
	SUBCODE VARCHAR(30),
	TEST_NO INT,
	MARKS INT,
	PRIMARY KEY(USN , SUBCODE , TEST_NO),
	FOREIGN KEY(USN) REFERENCES STUDENT(USN),
	FOREIGN KEY(SUBCODE) REFERENCES SUBJECT(SUBCODE),
	FOREIGN KEY(TEST_NO) REFERENCES TEST(TEST_NO)
);


CREATE TABLE ATTENDENCE(
	USN VARCHAR(30),
	SUBCODE VARCHAR(30),
	ADATE DATE,
	STATUS VARCHAR(30),
	FOREIGN KEY(USN) REFERENCES STUDENT(USN),
	FOREIGN KEY(SUBCODE) REFERENCES SUBJECT(SUBCODE)
);

ALTER TABLE ATTENDENCE ADD CONSTRAINT PK1111 PRIMARY KEY(USN , SUBCODE);

CREATE TABLE PARENT
(
	USN VARCHAR(30),
	PNAME VARCHAR(30),
	CITY VARCHAR(30),
	PRIMARY KEY(USN,PNAME),
	FOREIGN KEY(USN) REFERENCES STUDENT(USN)
);